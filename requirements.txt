# ScanLens web app dependencies (CPU-only, suitable for Streamlit Community Cloud).
# torch is pulled from the CPU wheel index so the app runs without a GPU.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.2.2
numpy<2
pandas
matplotlib
seaborn
scikit-learn
pillow
tqdm
streamlit
